// Lección 3: 11 Variables y Constantes

import Foundation

// Variable y constante son contenedores o espacios de memoria que pude almacenar datos
// variables es un espacio de memoria que puede varia a lo largo de la ejecución del programa jewmplo:

var str = "hola mundo"
//las variable se escrive con la palabra reserbada "var" el nomombre seguida de un signo "=" y el valor que queramos dale segun su tipo,
// Si ejecuto hasta aqui el playground, su volor en texto (string) sera de "hola mundo" 

// si cambiomos el valor por el seguiente:
str = "Cambio de valor"
// se puede ver que el valor en playground, a cambiado por en nuevo intropducido


// los datos puese de texto plano o string (volor de caractere unidos), como en el caso, de tipo numerico o boleano
//Para escribir las constantes utilizaremos la palabra reserbadaa "let", el nomombre seguida de un signo "=" y el valor que queramos dale segun su tipo
//las constante a diferencia de las variable es que su valor a lo largo de lña ejecucion del programa no cambia, tal como su nom bre indica, ejemplo:
let stringConstante = "Pedro"
// stringConstante = "Juas"
// al intentar cambiar el valor, Xcode lanza error dado que es una constante y no puede ser cambiada

